# name: Triển Khai GKE (SocialHub Microservices)

# on:
#   push:
#     branches:
#       - main # Kích hoạt khi push lên branch main (hoặc master)

# # Đảm bảo quyền ghi ID Token để Workload Identity Federation hoạt động
# permissions:
#   contents: read
#   id-token: write

# env:
#   PROJECT_ID: ${{ vars.GCP_PROJECT_ID || 'socialhub-micro-service-1' }}
#   GAR_LOCATION: ${{ vars.GCP_REGION || 'asia-east1' }}
#   GAR_REPOSITORY: ${{ vars.GAR_REPOSITORY || 'socialhub-repo' }}
#   GKE_CLUSTER: ${{ vars.GKE_CLUSTER || 'socialhub-gke-cluster' }}
#   GKE_REGION: ${{ vars.GCP_REGION || 'asia-east1' }}
#   # Đăng nhập bảo mật sử dụng GitHub Variables được cấu hình ở Cài đặt Repo
#   WIF_PROVIDER: ${{ vars.WIF_PROVIDER }}
#   WIF_SERVICE_ACCOUNT: ${{ vars.WIF_SERVICE_ACCOUNT }}

# jobs:
#   # ─────────────────────────────────────────────────────────────────────────
#   # JOB 1: BUILD & PUSH DOCKER IMAGES (Sử dụng Matrix để chạy song song 7 VMs miễn phí của GitHub)
#   # ─────────────────────────────────────────────────────────────────────────
#   build-and-push:
#     runs-on: ubuntu-latest
#     strategy:
#       matrix:
#         service:
#           - name: frontend
#             path: ./frontend
#           - name: gateway
#             path: ./gateway
#           - name: user-service
#             path: ./services/user-service
#           - name: friend-service
#             path: ./services/friend-service
#           - name: post-service
#             path: ./services/post-service
#           - name: media-service
#             path: ./services/media-service
#           - name: notification-service
#             path: ./services/notification-service
#           - name: chat-service
#             path: ./services/chat-service
#     steps:
#       - name: Checkout mã nguồn
#         uses: actions/checkout@v4

#       - name: Đăng nhập Google Cloud qua WIF (Workload Identity Federation)
#         uses: google-github-actions/auth@v2
#         with:
#           project_id: ${{ env.PROJECT_ID }}
#           workload_identity_provider: ${{ env.WIF_PROVIDER }}
#           service_account: ${{ env.WIF_SERVICE_ACCOUNT }}

#       - name: Cấu hình Google Cloud SDK
#         uses: google-github-actions/setup-gcloud@v2
#         with:
#           project_id: ${{ env.PROJECT_ID }}

#       - name: Cấu hình xác thực Docker cho Artifact Registry
#         run: |
#           gcloud auth configure-docker ${{ env.GAR_LOCATION }}-docker.pkg.dev --quiet

#       - name: Thiết lập Docker Buildx (Để tối ưu hóa cache build)
#         uses: docker/setup-buildx-action@v3

#       - name: Build và Push Docker Image
#         uses: docker/build-push-action@v5
#         with:
#           context: ${{ matrix.service.path }}
#           push: true
#           tags: |
#             ${{ env.GAR_LOCATION }}-docker.pkg.dev/${{ env.PROJECT_ID }}/${{ env.GAR_REPOSITORY }}/${{ matrix.service.name }}:${{ github.sha }}
#             ${{ env.GAR_LOCATION }}-docker.pkg.dev/${{ env.PROJECT_ID }}/${{ env.GAR_REPOSITORY }}/${{ matrix.service.name }}:latest
#           cache-from: type=gha
#           cache-to: type=gha,mode=max

#   # ─────────────────────────────────────────────────────────────────────────
#   # JOB 2: DEPLOY TO GKE AUTOPILOT
#   # ─────────────────────────────────────────────────────────────────────────
#   deploy:
#     runs-on: ubuntu-latest
#     needs: build-and-push # Chỉ chạy sau khi cả 7 Matrix build ở trên chạy xong
#     steps:
#       - name: Checkout mã nguồn
#         uses: actions/checkout@v4

#       - name: Đăng nhập Google Cloud qua WIF
#         uses: google-github-actions/auth@v2
#         with:
#           project_id: ${{ env.PROJECT_ID }}
#           workload_identity_provider: ${{ env.WIF_PROVIDER }}
#           service_account: ${{ env.WIF_SERVICE_ACCOUNT }}

#       - name: Cấu hình Google Cloud SDK
#         uses: google-github-actions/setup-gcloud@v2

#       - name: Đăng nhập và lấy thông tin cấu hình GKE
#         uses: google-github-actions/get-gke-credentials@v2
#         with:
#           cluster_name: ${{ env.GKE_CLUSTER }}
#           location: ${{ env.GKE_REGION }}

#       - name: Thay thế Image Tag trong các file Kubernetes Manifests
#         run: |
#           # sed -i "s|IMAGE_PLACEHOLDER_FRONTEND|${{ env.GAR_LOCATION }}-docker.pkg.dev/${{ env.PROJECT_ID }}/${{ env.GAR_REPOSITORY }}/frontend:${{ github.sha }}|g" k8s/frontend.yaml
#           sed -i "s|IMAGE_PLACEHOLDER_GATEWAY|${{ env.GAR_LOCATION }}-docker.pkg.dev/${{ env.PROJECT_ID }}/${{ env.GAR_REPOSITORY }}/gateway:${{ github.sha }}|g" k8s/gateway.yaml
#           sed -i "s|IMAGE_PLACEHOLDER_USER_SERVICE|${{ env.GAR_LOCATION }}-docker.pkg.dev/${{ env.PROJECT_ID }}/${{ env.GAR_REPOSITORY }}/user-service:${{ github.sha }}|g" k8s/user-service.yaml
#           sed -i "s|IMAGE_PLACEHOLDER_FRIEND_SERVICE|${{ env.GAR_LOCATION }}-docker.pkg.dev/${{ env.PROJECT_ID }}/${{ env.GAR_REPOSITORY }}/friend-service:${{ github.sha }}|g" k8s/friend-service.yaml
#           sed -i "s|IMAGE_PLACEHOLDER_POST_SERVICE|${{ env.GAR_LOCATION }}-docker.pkg.dev/${{ env.PROJECT_ID }}/${{ env.GAR_REPOSITORY }}/post-service:${{ github.sha }}|g" k8s/post-service.yaml
#           sed -i "s|IMAGE_PLACEHOLDER_MEDIA_SERVICE|${{ env.GAR_LOCATION }}-docker.pkg.dev/${{ env.PROJECT_ID }}/${{ env.GAR_REPOSITORY }}/media-service:${{ github.sha }}|g" k8s/media-service.yaml
#           sed -i "s|IMAGE_PLACEHOLDER_NOTIFICATION_SERVICE|${{ env.GAR_LOCATION }}-docker.pkg.dev/${{ env.PROJECT_ID }}/${{ env.GAR_REPOSITORY }}/notification-service:${{ github.sha }}|g" k8s/notification-service.yaml
#           sed -i "s|IMAGE_PLACEHOLDER_CHAT_SERVICE|${{ env.GAR_LOCATION }}-docker.pkg.dev/${{ env.PROJECT_ID }}/${{ env.GAR_REPOSITORY }}/chat-service:${{ github.sha }}|g" k8s/chat-service.yaml

#       - name: Triển khai các cấu hình và tài nguyên lên GKE
#         run: |
#           kubectl apply -f k8s/configmap.yaml
#           kubectl apply -f k8s/secrets.yaml
#           kubectl apply -f k8s/
